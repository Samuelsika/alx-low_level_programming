#include <stdio.h>

/**
 *main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c;

	int h;

	c = 'a';
	h = 0;
	while
		(h < 10) {
			putchar(h + '0');
			h++;
		}
	while
		(c <= 'f') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
